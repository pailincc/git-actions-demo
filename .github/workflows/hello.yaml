name: Hello World
on: 
    push:
        branches: [main]
    pull_request:
        branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello from GitHub Actions!"