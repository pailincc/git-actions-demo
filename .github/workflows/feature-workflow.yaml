name: Workflow for Feature Branch
on:
  push:
    branches: [feature/**]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Check new feayure quality
        run: echo "Start unit testing"
